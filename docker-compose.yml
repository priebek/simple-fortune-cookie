version: "3.1"
services:
    frontend:
        image: robinlp1/cookie-frontend:latest
        ports:
            - 8080:8080
    backend: 
        image: robinlp1/cookie-backend:latest
        ports:
            - 9000:9000